# AI-based-IDS
This project builds an AI-based Intrusion Detection System using the NSL-KDD dataset to classify network traffic as normal or attack. Multiple ML and DL models (SVM, KNN, RF, XGBoost, ANN) are evaluated to enhance network security.
